@{
    ViewData["Title"] = "Reset Password";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<div class="page-head">
    <h1>Set a New Password</h1>
    <p>Choose a new password for your account.</p>
</div>

<div class="form-card">
    @if (ViewBag.Error != null)
    {
        <p style="color:#ff6b6b; margin-bottom:20px;">@ViewBag.Error</p>
    }

    <p style="color:var(--steel); font-size:12px; margin-bottom:20px; font-family:'Space Mono',monospace;">
        Requirements: at least 8 characters, an uppercase letter, a lowercase letter, a number, and a special character.
    </p>

    @using (Html.BeginForm("ResetPassword", "Account", FormMethod.Post))
    {
        @Html.AntiForgeryToken()
        <input type="hidden" name="token" value="@ViewBag.Token" />

        <div class="form-row">
            <label for="newPassword">New password</label>
            <input type="password" id="newPassword" name="newPassword" required="required" minlength="8" autofocus="autofocus" />
        </div>
        <div class="form-row">
            <label for="confirmPassword">Confirm new password</label>
            <input type="password" id="confirmPassword" name="confirmPassword" required="required" minlength="8" />
        </div>
        <button type="submit" class="submit-btn">Update Password</button>
    }
</div>
